# Database Configuration (Connection String)
# For Railway, Render, Heroku, or other cloud providers, use your connection string:
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# For local development:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/osrs_prices?sslmode=disable

# Server Configuration
PORT=8080

# Alternative: Individual database parameters (used as fallback if DATABASE_URL is not set)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=osrs_prices
# DB_SSLMODE=disable